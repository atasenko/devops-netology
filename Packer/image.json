{
  "builders": [
    {
      "type":      "yandex",
      "token":     "was here",
      "folder_id": "folder-id",
      "zone":      "ru-central1-a",

      "image_name":        "ubuntu-2004",
      "image_family":      "ubuntu",
      "image_description": "Ubuntu with nginx",

      "source_image_family": "ubuntu-2004-lts",
      "subnet_id":           "e9b03dubhe76mkdleqbn",
      "use_ipv4_nat":        true,
      "disk_type":           "network-ssd",
      "ssh_username":        "ubuntu"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "echo 'updating APT'",
        "sudo apt-get update -y",
        "sudo apt-get install -y nginx",
        "sudo su -",
        "sudo systemctl enable nginx.service",
        "curl localhost"
      ]
    }
  ]
}
